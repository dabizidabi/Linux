-------DISK-------
/etc/fstab
lsblk
blkid
fdisk -l (/dev/sda)
mkfs -t (type) /dev/sda
mkfs.ext4 /dev/sda
mount -t (type) 
umount
e2label /dev/sda "Name Label"


------cron--------




------find--------
find /home -type f -name "file.txt" -exec chmod 0700 '{}' ';' 
(-perm, xargs)
-maxdepth <number_level> 
find /home/test -type f -name 'file-a' -exec tar rf test.tar '{}' '+'

-----zip------
find /home/test -type f -name 'file-a' | zip -@ file-A.zip
