<source>
  @type tail
  format json
  read_from_head true
  tag docker.logs
  path /tmp/log/containers/*/*-json.log
  pos_file /tmp/container-logs.pos
</source>

#<match docker.logs>
#  @type file
#  path /out/test.logs
#</match>
