---
- name: Install httpd web-server
  hosts: test_servers
  become: yes

  tasks: 
  - name: install Apache Webserver
    yum:  name=httpd state=latest

  - name: Apache start and Enable on boot
    service: name=httpd state=started enabled=yes

